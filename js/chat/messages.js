// messages.js
// Get messages from API and append to the chat box
// as well as send messages to API

import {url} from "./misc";

const token = localStorage.getItem('token');
const url = 'http://localhost:5000/api/v1/messages';

// Get messages from API using channel ID and community ID
export function getMessage(communityID, channelID) {
  // Requires message API to be completed (it's not)
  return 0;
}


















